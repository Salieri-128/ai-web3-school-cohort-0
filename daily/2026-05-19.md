# 2026-05-19 Daily Note

## 今日主题
- Handbook 章节：LLM / Prompt / Context / Agent
- WCB Learning 任务：AI Agent 入门 —— Hermes 从 0 到 1（已实时参加）
- 今日目标：把 Hermes Agent 调整成更适合 AI × Web3 School 学习流的个人 Learning Agent

## 今日最小路径（必须完成）
- [x] 阅读 Handbook 的 `LLM`、`Prompt`、`Context`、`Agent` 四个部分
- [x] 参加 `AI Agent 入门 —— Hermes 从 0 到 1` 直播
- [x] 把 Hermes Agent 接入微信并完成可用性调试
- [x] 梳理出 1 条 WCB 任务稳定读取路径
- [x] 将 starter prompt 转化为本地可复用 skill

## 推荐路径（有余力再做）
- [ ] 补读 `RAG`，把“为什么只靠上下文还不够”补完整
- [ ] 把 WCB 任务读取流程整理成可复用脚本或文档
- [ ] 把今天的 3 条关键收获浓缩成一版更短的 WCB proof 文案

## 挑战路径（进阶）
- [ ] 设计一个“WCB 任务读取 → Handbook 匹配 → daily note 生成”的 agent workflow
- [ ] 将 Hermes 的学习工作流扩展到 repo 自动整理 / submission draft / handbook feedback 生成
- [ ] 思考一个偏 Hackathon 的 Agent 方向：帮助新手理解链上任务、课程任务与 repo 沉淀之间的关系

## 今日笔记
- 今天系统阅读了 Handbook 的 `LLM`、`Prompt`、`Context`、`Agent` 四个部分，对“模型负责生成、prompt 决定任务表达、context 决定信息边界、agent 负责调度工具和状态”这条主线有了更清晰的理解。
- 我参加了 `AI Agent 入门 —— Hermes 从 0 到 1` 直播，把课程里的 agent 概念和我手上的 Hermes 实际工作流对上了。
- 今天最有价值的实践不是单纯看内容，而是直接把微信里的 Hermes Agent 调通，让它真正进入我的学习链路。
- 在调试过程中，我发现 Agent 虽然拿到了 WCB 的 API key，但不能稳定读出任务；继续排查后，确认对我这个账号更可靠的读取路径是：`users.getProfile -> events.listForLearner -> tasks.listForLearnerByIds`。
- 我把这条稳定读取路径存进了 Hermes 的 memory，避免以后每次都重新踩坑。
- 另外我重新审视了 AI × Web3 School 的 starter prompt，发现只把 prompt URL 放进 memory 不足以保证后续稳定执行，于是把 starter prompt 进一步整理成了本地 skill `ai-web3-school-learning-agent`。
- 通过“修任务读取路径”和“skill 化 starter prompt”这两步，我对 agent 的理解从“会用一个工具”推进到了“会改进 agent 的工作流与状态管理”。

## 今日实验 / 小练习
- 实验目标：让 Hermes Agent 更稳定地服务 AI × Web3 School 学习流程
- 过程：接入微信使用链路；排查 WCB 任务读取失败问题；验证正确读取任务的 API 路径；把稳定路径存入 memory；把 starter prompt 抽取为可复用 skill
- 结果：Hermes Agent 已经可以更稳定地配合我读取 WCB 任务、结合 Handbook 制定学习计划，并在后续会话中更一致地遵循 AI × Web3 School 学习工作流
- 遇到的问题：`tasks.listForLearner` 对我的账号并不可靠；starter prompt 只在最初 session 有强影响，如果不做 skill 化，后续执行会越来越依赖临时上下文

## Handbook feedback 候选
- 页面链接：https://aiweb3.school/learning-agent.zh.txt
- 问题：starter prompt 同时混合了“首次 onboarding 信息”和“长期 daily workflow 规则”，对已经完成初始化的学员来说，有些内容会变成噪音
- 建议：把 starter prompt 拆成“首次初始化附录”和“日常学习 workflow 主体”，让 Agent 更容易长期稳定执行核心规则
- 来源：我今天在实际使用 Hermes Agent 时，发现只保留 URL 和记忆摘要不足以稳定复用 starter prompt，需要进一步 skill 化

## 打卡草稿
今天我完成了 AI × Web3 School 里比较关键的一次“从会用 Agent 到会调 Agent”的练习。内容上，我阅读了 Handbook 的 `LLM`、`Prompt`、`Context`、`Agent` 四个部分，也参加了 `AI Agent 入门 —— Hermes 从 0 到 1` 直播，把理论和实际工具使用串了起来。

今天最重要的实践是把微信里的 Hermes Agent 调通，并继续排查它在 WCB 学习任务读取上的问题。我发现它虽然拿到了 WCB 的 API key，但不能稳定读出任务，最后定位并验证了对我这个账号更可靠的读取路径：`users.getProfile -> events.listForLearner -> tasks.listForLearnerByIds`，然后把这条路径存进了 memory。

另一个重要动作是我重新审视了 AI × Web3 School 的 starter prompt，发现只记住 prompt URL 还不够，所以我进一步把它转化成了本地 skill，让 Agent 后续在处理 WCB、Handbook、repo 和 daily planning 时能更稳定地遵循这个 workflow。

我今天最大的收获是：Agent 不只是“拿来用”的工具，也可以被持续调试和改进。通过修正任务读取路径和 skill 化 starter prompt，我对 agent 的理解从“会调用”推进到了“会优化工作流与状态管理”。

我接下来的下一步是：补读 `RAG`，并继续把 Hermes 工作流沉淀到 repo 里，让它更稳定地支持后续的 AI × Web3 学习和 Hackathon 准备。

### 打卡短版
今天我读了 Handbook 的 `LLM`、`Prompt`、`Context`、`Agent`，也参加了 `AI Agent 入门 —— Hermes 从 0 到 1` 直播。实践上，我把微信里的 Hermes Agent 调通，并排查出 WCB 任务读取的稳定路径：`users.getProfile -> events.listForLearner -> tasks.listForLearnerByIds`，还把 starter prompt 转成了本地 skill。今天最大的收获是，我开始从“会用 Agent”走向“会调试和优化 Agent 工作流”。

## 外部提交记录
- WCB 任务：Week 1｜线上活动｜实时参加 5.19｜AI Agent 入门：Hermes 从 0 到 1
- 提交状态：已提交
- 备注：WCB 中该直播实时参加任务当前状态为 `SUBMITTED`；如后续需要增强 proof，可基于本篇 daily note 进一步精炼补充文案
